export type Section1Settings = {
  panel: {
    heading: string
    hint: string
  }
  topics: {
    count: number
    titleFormat: string
  }
}

export type Section1Topic = {
  id: string
  title: string
}
